@model IEnumerable<CameraShop.Models.ThongSo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Admin.cshtml";
}
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            MaThongSo
        </th>
        <th>
            TenThongSo
        </th>
        <th>
            TinhTrang
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaThongSo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenThongSo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TinhTrang)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit",       new {id=item.MaThongSo }) |
            @Html.ActionLink("Details", "Details", new {id=item.MaThongSo }) |
            @Html.ActionLink("Delete", "Delete",   new {id=item.MaThongSo })
        </td>
    </tr>
}

</table>
